Make workflow for:

- bundling source code `Astrocosmos-{{VERSION}} ({{TYPE}}).{{ANY SUPPORTED COMPRESSED FILE FORMAT}}`
  - whole repo. TYPE = source_code
  - content, docs, assets, and templates. TYPE =
  - content only. TYPE = content_only 
- compile content, docs, assets, and templates into a book
- compress Celestia Addon folder 
- 
